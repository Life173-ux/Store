namespace Store
{
    class Bun : Product
    {
        public Bun(string name, string company, int price)
        {
            Name = name;
            Company = company;
            Price = price;
        } 
    }
}